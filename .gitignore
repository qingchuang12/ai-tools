# Dependencies
node_modules/
package-lock.json
# pnpm-lock.yaml 由 .gitignore 解除忽略：提交它以保证 GitHub Actions/CI 可复现与依赖缓存可用
#（此前忽略导致 setup-node 的 cache: pnpm 在 CI 找不到 lockfile 而失败）。

# Build outputs
dist/
build/
release/
release_verify/

# IDE
.idea/
.vscode/
*.swp
*.swo
*.iml
.codebuddy
.workbuddy
# MCP 客户端（ZCode）本机运行时目录，防误入库
.zcode/
.tmp
%TEMP%
# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Temporary scripts & helper files (agent/debug scaffolding, not project source)
tmp-*.ps1
*-tmp.ps1
*.tmp
dev-*.log
run-*.ps1
install-*.ps1

# Environment
.env
.env.local
.env.*.local

# Cache
.cache/
.parcel-cache/

# Lockfiles: project uses pnpm only
package-lock.json

# Debug / agent scaffolding residue
_verify/
*.bak
application.pid
